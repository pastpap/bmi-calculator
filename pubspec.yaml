name: bmi_calculator
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  font_awesome_flutter: ^10.1.0

dev_dependencies:
  test: ^1.21.4
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.0"

flutter:
  uses-material-design: true
  assets:
    - assets/json/bmi_children.json

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
